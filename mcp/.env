# GSC MCP Server configuration
# Edit these values, then restart the server for changes to take effect.

# Port the MCP server listens on
MCP_PORT=3301

# Default number of rows returned per tool call (keeps AI context windows manageable)
DEFAULT_RESULT_LIMIT=50

# Hard ceiling on rows returned — AI cannot request more than this
MAX_RESULT_LIMIT=200
